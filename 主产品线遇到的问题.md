# 主产品线遇到的问题

日期：2026-08-27

状态：持续追加。本文件记录主产品线已经讨论并由用户确认的关键问题、选择、理由、备选方案和代价；尚未确认的内容不得写成最终决定。代码、测试和Git仍是实现事实来源。

## DEC-001：Role如何分配给Agent，Agent如何绑定Model

### 问题

系统同时接入DeepSeek、Qwen和Kimi。Planner、Developer、Reviewer等Role应该永久属于某个Agent，还是由Runtime按任务从多个Agent中选择？Agent的个性与能力又应如何保持稳定？

### 最终策略

用户确认采用 **Role与Agent动态解耦，Agent与主要Model Policy间接、版本化绑定**。

- Role定义一次任务需要的职责、约束、能力和输出合同，不永久属于某个Agent。
- Agent Profile定义稳定身份、专长、能力、工具/Context/预算策略和主要Model Policy；AgentInstance绑定不可变Profile版本。
- Runtime为Task/Invocation创建持久`RoleAssignment`，从合格Agent中选择执行者并记录候选摘要、选择理由和版本。
- 一个Role可由多个Agent承担；一个Agent也可在不同任务承担不同Role。
- Agent不能在当前Session中静默换模型；实际Invocation必须记录Provider、Model、版本和Usage。
- 模型与Agent都只能提交候选输出；Runtime-owned Validator和用户保留最终裁决边界。

### 为什么选择它

Role是任务需求，Agent是可调度执行者。把两者解耦后，Runtime可以优先派最适合的Agent；最佳Agent忙时，可以选择仍满足能力、权限、工具、Context、Provider健康和预算硬门槛的第二候选，而不是让任务永久等待或把Role锁死。

Agent通过Profile绑定主要Model Policy，可以保持个性、专长和行为连续性。跨模型Review由绑定不同Model Policy的独立Agent承担同一Reviewer Role完成，而不是让同一Agent临时“换脑子”。

### 其他方案对比

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| Role永久绑定一个Agent | 实现最简单，职责一眼可见 | 最佳Agent忙碌/故障时Role停摆；无法在多个合格Agent间调度 | 不采用 |
| Role与Agent动态分配，Agent绑定Profile/主要Model Policy | 兼顾任务调度、Agent稳定个性、跨模型Review和故障替代 | 需要RoleAssignment、候选排序、选择证据与迁移 | **第一版采用** |
| Role直接选择Model，不保留稳定Agent | 路由灵活、对象较少 | Agent退化为Mailbox标签，个性/记忆漂移，用户无法形成稳定预期 | 不采用 |
| Agent自由抢任务 | 中央调度少，表面自治 | 难以保证权限、优先级、公平、去重和可复现 | 不采用 |

### 调度边界

1. 先做硬过滤：能力、权限、工具、Context、Provider健康和预算任一不满足即不能候选。
2. 再做软排序：任务适配度、可用性、历史质量、成本/延迟和当前负载。
3. 最佳Agent忙时只能选合格次优；没有合格Agent时排队或进入`needs_input`，不能为追求速度强派。
4. 同分使用稳定规则，候选摘要和选择理由必须持久化，确保复现。
5. Assignment执行中不能静默换Agent；重新分配必须创建新Assignment/Handoff并保留因果链。

### 当前边界与验收

该纵切已经实现：`AgentProfile.role_ref`现在可为空且旧payload继续兼容；Runtime新增任务级`RoleRequirement`、候选证据、确定性Scheduler、不可变`RoleAssignment`、SQLite v6 Store与Assignment+Mailbox同事务投递。AgentInstance/Session、Mailbox、FIFO/并行、Message/Handoff和Validator没有重写。

当前代码已证明：同一Role有多个候选时按稳定规则选择；普通任务最佳Agent忙时只选择通过硬门槛的次优；高风险/强连续性任务按显式Policy等待或进入`needs_input`；不合格Agent不会因空闲获选；同代并发写入只成功一次；重新分配必须创建下一代并引用旧Assignment；提交时会复核所选Agent/Session/Profile快照。实际Model/Usage和前端展示仍要在后续Provider/API/Web纵切接入。

## DEC-002：Agent之间采用什么通信拓扑

### 问题

Agent需要独立判断、互相质疑和交接，但完全自由直连容易产生重复触发、无限乒乓、越权、跨Thread污染和无法取消；如果所有判断都交给一个Boss Agent，又会形成单点瓶颈和认知锚定。

### 最终策略

用户确认采用 **Runtime中介的受控网状拓扑**：

- **判断层逻辑对等**：Agent可以独立提出方案、质疑其他Agent并发起Handoff，没有一个Boss Agent拥有跨角色的最终判断权。
- **通信层集中治理**：Agent不能私下直连。所有`SEND_MESSAGE`、`HANDOFF`和后续用户交互意图必须经过Runtime Router与持久Mailbox。
- **执行层结构化推进**：稳定主流程由SOP/DAG表达依赖，运行中允许Runtime批准的动态Handoff边。
- **默认一对一**：多接收者必须显式列出；第一版禁止Agent向全体自由广播。
- **发布订阅限于事件投影**：Agent状态、Message已持久化、Artifact更新等Runtime事件可以推送给前端；任务正文不通过无目标Pub/Sub扩散。
- **黑板只保存已验证事实**：共享事实区是辅助Context来源，不是所有Agent共享整段聊天记录的大黑板。

逻辑路径如下：

```text
Agent A
  → 提交结构化通信意图
  → Runtime检查Thread、身份、权限、预算、接收者、重复与链路边界
  → 先持久化Message/Handoff
  → 投递到Agent B的Mailbox
  → Context Compiler为Agent B生成最小相关上下文
  → Runtime调用Agent B所配置的模型
```

使用API模型时，模型进程并不永久在线。持续存在的是Runtime中的Agent身份、Session、Mailbox和状态；只有Agent收到可执行消息时，Runtime才调用相应模型。

### 为什么选择它

该方案把“Agent拥有独立判断”与“系统必须可控”分开处理。Agent可以对等协作，不受Boss结论锚定；Runtime仍能统一执行身份检查、权限、顺序、预算、去重、取消、持久化和审计。

它也直接复用现有SQLite Mailbox、同Agent FIFO、跨Agent并行和结构化Handoff底座，不需要另建第二套消息系统。

### 其他方案对比

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| 中央Boss/Master星型拓扑 | 流程直观，任务分配容易 | Boss成为性能和认知单点；所有上下文集中污染；其他Agent容易被锚定；Boss错误会扩散全局 | 不作为跨Agent判断层 |
| Agent完全自由的全网直连 | 自由度高、表面延迟低 | 难以统一权限、去重、顺序、取消和审计；容易重复触发、无限乒乓和跨Thread污染 | 不采用 |
| 纯发布订阅 | 松耦合，适合状态广播 | 接收者与因果关系隐含；容易形成消息风暴；任务交接和责任归属不清 | 只用于Runtime/UI事件 |
| 全局大黑板 | 所有Agent都能看到全局信息 | 冗余、过期事实、权限泄漏和上下文污染严重；模型容易把讨论当事实 | 只保留“已验证事实黑板”作为辅助 |
| Runtime中介的受控网状拓扑 | 兼顾对等判断、持久化、权限、审计、取消和动态协作 | Runtime Router成为关键组件；需要明确协议、去重和收敛规则 | **采用** |

### 所选方案的代价

Runtime Router会成为单机第一版的关键组件。如果Router停机，新的通信无法推进；如果规则写错，可能统一阻塞所有Agent。第一版接受这个单机边界，但必须让消息先持久化、失败可见、Router逻辑可测试。高可用、跨进程投递、ACK/重试和崩溃重投属于后续生产路线，不能冒称当前已经解决。

### 当前边界与验收

1. Agent不得绕过Runtime直接调用另一个Agent或向其Provider发送任务。
2. 每条消息必须绑定Thread、发送者、接收者和因果引用；完整字段在下一项“通信协议”讨论中冻结。
3. 同Agent继续FIFO，跨Agent允许并行；pause/resume/close必须影响调度。
4. Runtime先持久化再投递，前端只投影公开状态，不展示模型私有推理。
5. 第一版必须用故障实验复现并阻止双重触发、无限乒乓、不可取消和跨Thread污染。

## DEC-003：团队任务由谁理解和分配

### 问题

用户没有`@`具体Agent、而是把任务交给整个团队时，应该由Planner理解和拆分，还是让Runtime直接从原始任务中选择Agent？两者都在广义Harness中，但职责和可信边界不同。

### 最终策略

用户确认采用 **Planner负责业务语义，Runtime负责具体执行分配**。

- 所有用户Message技术上先由Runtime持久化并检查Thread和权限。
- 没有`@`的团队任务，Harness先请求Planner Role，Runtime通过RoleAssignment选择具体Planner Agent。
- Planner理解用户目标、澄清信息、拆分子任务、建立DAG，并提交RoleRequirement、依赖和验收条件。
- Planner不得直接指定AgentInstance、Session或Provider，也不得扩大权限和预算。
- Runtime不解释业务目标、不生成计划；它根据RoleRequirement和真实状态选择具体Agent/Session/Profile，验证后投递Mailbox。
- 用户直接`@`一个Agent时走单播；显式`@`多个Agent时走独立多播，默认不经过Planner，也不自动让接收者互相协作。

```text
Planner：做什么、为什么做、先后顺序、需要什么Role
Runtime：由谁执行、何时执行、能否执行、是否允许执行
Agent：完成已分配的子任务并提交Action/Artifact
Validator：根据冻结标准判断候选是否通过
```

### 为什么选择它

Planner使用模型理解开放的业务语义，适合处理歧义、分解和依赖；Runtime使用确定性Policy读取Agent能力、Session状态、Provider健康、权限、Context、预算、负载和成本/延迟，适合做可测试、可审计的选择。分权允许Planner提出灵活方案，同时防止模型凭空指定不存在、不合格或越权的Agent。

已完成的RoleAssignment纵切承接这一边界：Planner产生RoleRequirement，Runtime-owned Scheduler生成持久RoleAssignment，再与Mailbox同事务投递。

### 其他方案对比

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| Planner理解任务并直接指定具体Agent | 灵活、调用链短 | Planner可能依据过期或虚构状态选人，绕过权限、预算和确定性选择证据 | 不采用 |
| Runtime直接理解原始任务并选择首个Agent | 表面自动化程度高 | Runtime被迫内置分类模型或隐藏Planner，变得不确定、难测试并混淆权限边界 | 第一版不采用 |
| 团队任务广播给全部Agent | 实现简单，每个Agent都有机会参与 | 重复成本高、责任不清、上下文污染和自由讨论死循环 | 不采用 |
| Planner提出RoleRequirement，Runtime选择具体Agent | 语义判断与执行权分离，复用持久Assignment，既灵活又可控 | 多一步合同与持久化，需要处理无效计划和无合格Agent | **采用** |

### 路由规则

```text
没有@ / 团队模式
→ Runtime持久化
→ 请求Planner Role
→ Runtime选择具体Planner Agent
→ Planner拆解并提交RoleRequirement
→ Runtime分配后续Agent

@一个Agent
→ Runtime验证后直接单播

@多个Agent
→ Runtime生成独立Delivery
→ 接收者先独立判断
→ 不自动建立Agent间协作
```

### 当前边界与验收

1. Planner输出必须是结构化DAG、RoleRequirement和验收条件，不能用自然语言点名具体Agent替代分配合同。
2. Runtime只能从满足硬能力、权限、工具、Context、Provider健康和预算条件的候选中选择。
3. 无合格Agent时必须排队、`needs_input`或明确失败，不能让Planner捏造新Agent或静默降低硬门槛。
4. 用户显式指定Agent时仍需Runtime检查Scope、状态和权限；`@`不是越权通道。
5. Runtime拒绝Planner方案时必须返回结构化原因，让Planner修订DAG或请求用户，而不是无限重试。

## DEC-004：产品主线采用小步冻结与垂直切片

### 问题

通信、Context、协作、收敛和前端合同是否必须全部设计完才可以编码？一次性设计能减少局部返工，但在真实模型尚未接入时，大量细节只是假设，容易耗时且过度设计。反过来完全边写边猜，又会反复改Schema和权责边界。

### 最终策略

用户确认采用 **小步冻结＋垂直切片**：每次只冻结一个当前切片不可缺少的最小决策，随后立即做完整链路、测试和一次真实API验证。记录遇到的错误、成本、延时、模型差异和调优，用证据决定下一项。

如果实践证明已冻结的决策不合适，保留原决策和当时理由，再追加修订记录；不把历史改写成一条没有踩坑的理想路径。

### 为什么选择它

这个方法在当前单用户、三个Provider和本地Web首版场景中，能更早用真实模型暴露结构化输出、超时、限流、上下文和路由问题；同时又通过“实现前先冻结最小边界”避免核心接口每次随意变动。它比“全部设计完再做”更快取得证据，也比“没有合同直接写”更容易测试和回滚。

### 执行边界

1. 一个切片必须贯通真实的用户/模型输入和Runtime可观察结果，不以新增一个数据类便宣称完成。
2. 功能按TDD推进；Fake Provider用于快速红绿和故障注入，每个可运行纵切结束时再做至少一次真实Provider API验证。
3. 真实API需要当次网络、Key和费用授权；API Key不得进入Message、SQLite私有状态、日志、前端或模型上下文。
4. 小步不允许绕过Scope、权限/私密、因果、幂等和最小终止边界；不为还没有真实需求的复杂Handoff生命周期、分布式队列或通用Agent市场提前建模。
5. 每个切片验证后先向用户汇报实际效果与偏差，再进入下一项。

## DEC-005：第一个Agent Action只实现SEND_MESSAGE

### 问题

首次实现Agent Loop时，是否应该一次建好Message、Delegate、Finish、AskUser和完整Handoff生命周期？全做能提前统一接口，但会同时引入任务所有权、回复链、收敛、Context和前端状态，当前没有真实调用证据证明这些复杂度有收益。

### 最终策略

用户确认首切只实现`agent-action/v1 send_message`：

```json
{
  "schema_version": "agent-action/v1",
  "action": "send_message",
  "recipient_role": "reviewer",
  "content": "请检查这份方案是否存在遗漏。"
}
```

模型只表达收件Role和自然语言内容。Thread、发送者、具体接收Agent/Session、Message/Assignment/Requirement ID、时间、顺序、父消息、因果和幂等身份均由Runtime从当前Invocation上下文生成。模型不能点名具体Agent，RoleAssignment从当前合格候选快照中确定接收者。

首切只支持单Role、单文本、单Action和单轮投递。成功写入Message/Mailbox后当前调用结束，接收Agent不自动调用模型。因此它是通信，不是任务责任转移；`DELEGATE`和独立Handoff在真实需要时再设计。

### 失败、延时和成本边界

- Runtime先检查Thread状态，非`open`直接拒绝，不调用模型。
- 协议字段无效时最多追加一次结构化修正请求；再次无效进入`protocol_error`，不写入半条Assignment或Message。因此正常路径一次模型调用，协议修正路径最多两次。
- Role无合格Agent时持久`needs_input/no_eligible_agent` Assignment，不让模型循环重试。
- 幂等身份由`scope_id + invocation_id + step_index`派生；已完成重放直接返回原Message，不再调用模型，不重复投递。
- API Key只从后端环境变量读取，不进入Action、Message、SQLite、日志或模型上下文。

### 为什么不一次实现全部Action

`SEND_MESSAGE`已能证明最关键的真实接缝：模型结构化输出、Runtime严格校验、Role到Agent选择、原子持久和Mailbox可见。额外Action会引入新语义和更多API调用，但不会让这条基础链的结论更真实。因此先实践、再决定下一Action，比预建通用协议更符合当前成本、时间和学习目标。

### 当前实践结果

离线TDD纵切已实现在`demo/coding_workflow/agent_actions.py`，公共行为测试在`demo/tests/test_send_message_action.py`。首批六个用例覆盖成功投递、一次修正、二次无效、无合格Agent、幂等重放和暂停Thread；相关40项、Runtime 184项、全仓非expected-red 595项、编译、diff-check与离线portfolio smoke通过。首次真实DeepSeek smoke随后完成一次调用：JSON Action通过严格字段校验，未触发协议修正，输入169、输出42、总计211 Token，Provider延时1318 ms。模型返回的Role字符串没有命中候选映射中的规范键`reviewer`，所以Runtime持久化`needs_input/no_eligible_agent`并保持Mailbox为空。这证明Provider接入可用，也真实暴露了“模型看到的是自然语言Role名称，Router需要规范ID”的合同缺口。

## 已实施的防复发修复

首次真实smoke继续如实保留为未通过。用户确认的最小修订已经实现：Runtime每次根据当前`role_candidates`生成稳定排序的动态`recipient_role.enum`，并在系统Prompt列出相同ID，要求模型原样复制，禁止翻译、改写大小写、使用显示名称或创造Role。Runtime仍精确匹配，未知Role保持`needs_input`，没有增加大小写猜测、相似度或LLM二次路由。新增红测先因Schema缺少`enum`失败，最小实现后切片7/7、相关41/41、Runtime 184/184、全仓非expected-red 596/596（9 skip）、编译和diff-check通过。

修复后的真实复验已经通过：同一`deepseek-v4-pro`正常路径只调用一次，未触发协议修正；输入212、输出42、总计254 Token，Provider延时1310 ms。模型返回的Action使RoleAssignment稳定得到`assigned`，Runtime选择`reviewer-agent`，Message成功持久化，目标Mailbox恰有一条消息。这个对照说明修复作用在真正的模型—Runtime语义接缝，而不是用Runtime模糊猜测掩盖错误。`SEND_MESSAGE v1`至此完成；用户随后选择并冻结的下一项是一跳接收与ContextBundle，见DEC-006。

## DEC-006：接收Agent采用白名单ContextBundle和强制一跳

### 问题

Planner的Message进入Reviewer Mailbox后，是否立即把完整Thread历史交给Reviewer并让双方自由回复？这种方式像普通聊天室，但会同时带来上下文费用、历史污染、重复讨论和Agent乒乓；完全依赖用户手动点击又无法体现自动协作。

### 最终策略

Runtime在Planner成功投递后自动执行一个接收hop。初版Reviewer只获得`context-bundle/v1`七字段白名单：`task_goal`、当前`recipient_role`、一条`trigger_message`、`verified_facts`、`artifact_refs`、`constraints`和`allowed_actions`，不默认携带完整聊天记录或私有推理。真实smoke暴露证据不足后，DEC-007在同一白名单合同中增加必需`review_subject`。Reviewer本轮只能使用既有`SEND_MESSAGE v1`回复Planner一次；回复进入Planner Mailbox后`auto_continuation_scheduled=false`，不再自动启动Planner。

必需字段超过显式预算时返回`needs_input/context_overflow`，不调用模型，也不静默截断目标或触发消息。可选信息依次按“约束、已验证事实、Artifact引用”尝试加入；放不下的条目保留稳定`omitted_refs`供Runtime/UI说明。Token计数器由调用方注入，当前不宣称是DeepSeek/Qwen/Kimi统一精确Tokenizer。

### 为什么这样选择

它比完整历史直灌更便宜、可审计，也比手工启动更接近真实产品协作；一个自动hop和一个Action足以验证第一次双Agent交流，同时把无限循环的控制权留在Runtime。暂不增加`FINISH/ASK_USER/HANDOFF`，避免在真实双模型证据出现前预建完整状态机。

### 当前实践结果与边界

离线TDD已打通`Planner Action → Reviewer Mailbox → ContextBundle → Reviewer Action → Planner Mailbox → 停止`。六项行为覆盖自动一跳、白名单字段、重复执行不重复模型、暂停Session拒绝且不消费、必需上下文超限fail-closed、可选上下文稳定裁剪；相关32项、Runtime 184项和全仓非expected-red 602项（9 skip）通过。

接收Invocation ID由触发Message、接收Agent和hop确定生成，并在成功回复的Assignment/Message因果链中持久化；当前没有新增SQLite v7独立pending Invocation记录，仍继承Mailbox“领取即推进、无ACK/崩溃重投”的既有边界。在这个离线检查点，真实Planner＋Reviewer DeepSeek一跳smoke仍需用户另行授权；它随后已执行并暴露下述证据缺口。

### 真实双Agent smoke发现

用户随后实际运行真实DeepSeek一跳。Planner与Reviewer各调用一次、零协议修正；两个RoleAssignment均成功，父子Message正文一致，Reviewer Mailbox的触发消息已消费，Planner Mailbox的回复未消费，Runtime在一个hop后停止。输入Token分别为218和361，输出分别为52和65，Provider延时分别为1339 ms和2012 ms。通信、持久化和终止合同真实通过。

但Reviewer收到的`artifact_refs`为空，Context中也没有通信协议正文。它建议“明确指定JSON Schema以减少解析歧义”，而系统实际上已经通过动态JSON Schema约束Action。这不是一次有证据的协议评审，而是模型在缺少评审对象时给出的合理化泛建议。结论必须拆分为：`TRANSPORT PASS`，`REVIEW QUALITY INCONCLUSIVE`。

这个踩坑说明避免上下文污染不能只做减法。评审类任务必须绑定实际评审对象：要么Planner在Message中携带足够正文，要么Runtime提供可解析的不可变Artifact引用与受控内容；两者皆无时应进入`needs_input/missing_review_subject`，不能让Reviewer凭任务标题继续生成答案。该问题的后续决定与实现见DEC-007。

## DEC-007：评审任务必须绑定真实评审对象

### 问题

第一次真实双Agent smoke证明消息能双向传递，但Reviewer只看到“评审通信协议”的任务标题，没有协议正文，因而给出了听起来合理、实际与现有动态JSON Schema重复的建议。只限制历史长度可以减少污染，却不能保证上下文足以完成任务。

### 最终策略

评审对象成为ContextBundle的必需证据。短内容直接使用已持久化触发Message正文，标记为`review_subject.source=inline_message`；长内容使用该Message携带的版本化`core:artifact`引用，由Runtime注入的解析器受控读取，标记为`source=artifact`并同时提供引用和正文。模型不能自己声明要评审哪个Artifact，也不能在Runtime外私下读取。

两种来源都没有时返回`needs_input/missing_review_subject`；Artifact没有先随Message持久化时返回`needs_input/subject_artifact_unbound`；Artifact不可读取或为空时返回`needs_input/subject_artifact_unavailable`。这些状态都不消费Mailbox、不调用Reviewer。评审对象超过Context预算时返回`needs_input/context_overflow`，不能把对象裁掉后让模型继续猜。

### 为什么不只选一种

所有短文本都先制造Artifact会增加一次写入、引用解析和界面概念，收益很小；所有长文本都塞进Message又会造成重复存储、窗口浪费和版本不清。按内容大小选择载体，同时让Runtime使用同一`ReviewSubjectBinding`合同校验，成本和可审计性更平衡。

### 当前实践结果

离线TDD覆盖Message正文绑定、缺失对象fail-closed、Artifact持久引用解析、未绑定引用拒绝、来源规范化，以及既有必需Context超限、可选裁剪、幂等、暂停和一跳停止。当前纵切11/11、相关45/45、全仓非expected-red 607/607（9 skip）通过。

修复后的真实DeepSeek smoke也已通过。Planner Message与Reviewer `review_subject`逐字一致，Reviewer引用协议中“持久化Message、记录parent与causation”的具体内容再提出幂等建议，因此这次是有材料依据的评审。与此同时出现两个新认识：第一，协议摘要没写现有确定性message ID、Assignment重放与Mailbox防重复，导致模型把“文档未说明”推断成“系统未实现”；第二，同一协议正文同时进入`review_subject.content`和`trigger_message.content`，产生无收益的Token重复。前者应修正Context证据完整性，不能直接照单新增分布式去重；后者应在下一最小合同中去重。

用户最终选择兼容去重方案：不删除`trigger_message.content`，因为它是发送方原始Message正文，服务于审计、历史、重放和非评审通信；inline `review_subject`改为`content_ref="trigger_message.content"`，只声明哪段正文是评审对象。Artifact模式继续允许Trigger正文表达“请评审这个文件”，同时由`review_subject.content`携带独立解析的文件内容。这样模型只收到一份inline正文，也不会损害Trigger语义。该修改已按TDD离线完成，相关45/45、全仓607/607（9 skip）通过。

真实DeepSeek复验随后通过：公开Subject只有`content_ref`，Trigger正文完整，Reviewer成功沿引用读取并引用具体协议。Context估算从455降到378，Reviewer input从553降到520；本次正文同时变长，因此只把它作为去重方向有效的证据，不宣称精确节省比例。

Reviewer新建议“message ID加入content哈希，同Invocation的不同content当新消息”。它指出的风险真实存在：当前同一`scope + invocation + step`重放会直接返回原Message，没有检查调用方是否错误换了请求。但建议的方案不采用，因为幂等身份代表同一次操作；不同请求复用同一身份应显式冲突，而不是伪装成新消息。模型content又只在模型调用后产生，把它放入ID会破坏“重放不再次调用模型”的目标，并可能因模型非确定性制造重复。正确候选是持久化首次规范请求摘要，同key不同摘要返回`idempotency_conflict`。该项记录为后续双路由/幂等键误用故障实验，不阻塞DEC-007关闭。

## DEC-008：把CLI工程化自检转换为Provider API自检

### 问题

Cat Café第二课作业以CLI子进程为对象，使用stdout/stderr活动、SIGTERM/SIGKILL、父子进程清理和NDJSON解析判断工程可靠性。本项目确定使用DeepSeek、Qwen和Kimi API Key；如果原样照搬，就会把stderr沉默误当模型死亡，或对根本不存在的Provider子进程设计错误的kill逻辑。但完全丢弃这份作业，又会漏掉超时、流式分片、环境隔离和错误恢复等同构风险。

### 最终策略

用户确认把[第二课CLI工程化自检](https://github.com/zts212653/cat-cafe-tutorials/blob/main/docs/lessons/02-homework.md)改写后纳入`PRODUCT-01B`必过门槛：

- stdout/stderr活动改为HTTP请求、响应头、最后流式事件/数据块、完成和断连时间；stderr只保留给本地工具与后端脱敏日志。
- 单一进程超时改为连接、读取、单请求和任务总预算四层超时；没有日志不等于Agent死亡。
- Provider调用使用API取消和连接中断；`SIGTERM → 宽限期 → SIGKILL`只用于本地工具或失控Worker。
- NDJSON半行/粘包问题泛化为SSE或分块JSON的半帧、多帧、空帧、畸形帧、断流、部分响应与大小上限。
- 环境隔离扩展为endpoint、数据库、API Key和额度隔离；Key禁止进入日志、SQLite、Message、Artifact、Context和前端。
- 异常退出/重试扩展为连接失败、超时、429/`Retry-After`、5xx、Usage缺失、无效JSON、部分响应和取消竞态；所有重试受Invocation幂等与预算控制。
- Provider健康必须由Runtime从请求证据生成快照，并作为RoleAssignment硬过滤输入；模型不能自报健康。
- HTTP 2xx、收到文本或结构化JSON通过只证明传输成功，不能替代Artifact、Validator、独立Reviewer或用户的语义验收。

### 为什么这样选择

CLI和API的载体不同，但共同问题都是：如何确认执行仍在推进、如何安全停止、如何处理不完整输出、如何隔离秘密、如何避免失败重试制造第二次副作用。保留这些工程目标并替换错误的观测信号，可以真正复刻教程踩坑，而不是只复刻它当时使用的技术外壳。

### 计划归属与边界

Fake HTTP、分层超时、流解析、取消、错误、Key隔离和Provider健康在`PRODUCT-01B`实现；Invocation/AgentSession收到这些信号后的暂停、隔离、关闭与重新分配在`PRODUCT-01C`实现；本地工具的stderr和进程树清理继续使用现有`local_execution`边界；跨Provider真实故障与语义验收在`PRODUCT-01E`完成。本决定细化既有范围，不新增批次，01B仍估算4～6小时，差异返工使用既有风险缓冲。

## DEC-009：同一幂等身份不能绑定不同请求

### 问题

真实Reviewer指出：message ID只由`scope + invocation + step`派生时，如果调用方错误复用同一身份却更换请求，Runtime会返回旧Message。Reviewer建议把模型输出content加入ID并把不同content当新消息，但这会要求重放时再次调用非确定模型，反而可能制造重复。

### 最终策略

幂等身份仍表示同一次操作，不由模型输出决定。Runtime在模型调用前对规范请求信封生成摘要；首次Assignment通过其RoleRequirement ID持久保存该摘要。相同身份重放时，摘要一致才返回原Message；摘要不同则`rejected/idempotency_conflict`，不调用模型、不创建新Assignment/Message、不追加Mailbox。

摘要覆盖ModelRequest消息、能力和response schema，Thread/Turn/Invocation/发送者/parent/Artifact引用，允许的规范Role ID，以及Assignment Policy版本和等待值。图片正文不直接进入摘要信封，只记录Artifact引用、MIME/detail和数据SHA-256。动态候选Agent、忙闲、健康和负载不参与，因为这些运行态变化不能把同一次重放误判成新请求。SQLite只保存64位摘要作为opaque Requirement ID的一部分，不保存额外Prompt副本。

### 为什么这样选择

同一key不同请求是调用方协议错误，应显式暴露，不能静默返回旧结果，也不能伪装成新操作。新任务必须使用新Invocation或step。content属于模型调用后的非确定输出，不适合作为调用幂等身份；输入摘要既能在调用前比较，也不会增加Provider费用。

### 当前实践结果与边界

公共红测先复现“方案B错误得到方案A旧Message”，随后最小实现转为`idempotency_conflict`；Fake模型只允许调用一次，证明冲突路径零模型调用，Reviewer Mailbox仍只有首次消息。相同请求重放原有行为保持通过。SEND_MESSAGE 8/8、相关46/46、全仓非expected-red 608/608（9 skip）、编译和diff-check通过。

当前没有独立IdempotencyRecord表或请求摘要算法版本迁移；摘要借用RoleRequirement opaque ID持久化，适合尚无产品历史数据库的当前MVP。进入长期本地API历史前，需要冻结摘要算法版本和旧记录兼容策略。本切片没有真实Provider调用，因为被验证的关键门槛正是冲突发生在模型调用之前。

## DEC-010：产品主路径从Raw API Agent Loop修订为成熟Agent CLI

### 问题

使用DeepSeek、Qwen、Kimi API Key只能得到基础模型响应。若要让单个Agent稳定地读取文件、调用工具、观察结果、继续执行、压缩上下文并处理中断，项目还必须自行完善一套Agent Loop。这个工作会占用大量时间，也会把多Agent通信实验的结果和“单Agent Loop是否成熟”混在一起。

### 最终策略

第一版产品改为CLI-first：选择一个成熟Agent CLI作为`FullAgentBackend`，由它承担单Agent内部loop；本项目通过统一`AgentExecutor`向它提交一次有边界的Invocation并接收公开事件和最终结果。Runtime继续独占Planner拆分、RoleAssignment、Message/Mailbox、Context、Handoff、预算、终止、审计和Acceptance。CLI Agent不能私下调用另一个Agent、直接写Mailbox、扩大权限或自行宣布产品任务通过。

现有`ModelClient`、DeepSeek真实通信smoke和Provider API故障自检全部保留为`RawModelBackend`对照与后续学习材料。DeepSeek、Qwen和Kimi三家API不再是第一版Web产品的硬门槛；首版只接一个成熟CLI，并优先让核心Agent使用相同CLI/模型但隔离Session，以减少模型能力差异对协作机制观察的干扰。

### 为什么这样选择

这不是放弃Agent能力，而是明确购买与自研边界：成熟CLI提供已经可用的单Agent工具循环，项目把有限时间投入到真正的差异化部分——多个Agent如何分工、通信、共享受控上下文、解决冲突、收敛和接受人工介入。与继续自研Raw API Loop相比，CLI-first能更快形成真实产品；与让多个CLI自由互调相比，统一Runtime仍保留可测试、可审计和可替换性。

### 代价与保留问题

Agent Loop风险不会完全消失，而是转移到CLI集成边界：子进程生命周期、stdout/stderr或JSONL解析、超时与取消、权限/审批、Workspace隔离、Session恢复、版本漂移、认证和额度都必须测试。CLI退出0或声称完成不等于语义正确，仍需Artifact、Validator、独立Reviewer或用户证据。具体首个CLI、认证方式、权限策略、Session连续性和非交互输出合同由用户确认后再按TDD实现；未确认前不安装第二CLI或编写供应商专用生产代码。

用户随后确认首个Backend使用Codex CLI和ChatGPT订阅；Planner/Reviewer只读，Developer仅Workspace可写；每Thread每Agent独立且可恢复Session；公开工具、文件变化、测试、耗时和最终消息，隐藏私有推理、完整stderr和凭据。本机ChatGPT应用内置Codex CLI已登录，但没有加入shell PATH。Fake合同已经证明新Session和显式Session resume、JSONL公开事件、Usage与私有内容过滤；紧邻的受控Transport纵切又证明Prompt只走stdin、执行受一次性批准约束、argv只能使用固定安全参数，并复用仓库唯一进程Owner及清理屏障，没有新建第二套`subprocess`路径。

认证实践证明直接继承真实HOME没有必要也不安全：相同Codex在Harness私有HOME中返回`Not logged in`，只额外提供宿主`CODEX_HOME`路径后恢复`Logged in using ChatGPT`。因此桥接保持HOME/TMPDIR私有，只让Codex主进程定位自身认证/Session缓存；CLI固定启用`--ignore-user-config`、严格配置、core工具环境、默认秘密名排除和`CODEX_HOME`显式排除。认证文件内容从未读取、复制或记录。

首次真实Codex read-only smoke证明离线参数可解析不等于安全策略实际生效。一次调用成功返回Session、JSONL终态、shell事件与Usage，Sandbox为read-only，Agent也报告Workspace未修改；但shell检查得到`CODEX_HOME`存在，Runtime因此正确返回`SMOKE_ACCEPTANCE_FAILED`。没有输出变量值、认证内容、stderr或私有推理，也没有自动重试。官方当前配置参考把项目使用的`shell_environment_policy.exclude`标成legacy，建议新配置使用`filters`；这构成下一假设的依据，但一次模型观测不能区分“legacy过滤未生效”和“Agent误判工具结果”，所以尚未宣称根因或修复。真实代价也比Raw API短消息明显：22689 ms，输入30574、缓存输入22016、输出139、reasoning输出42。下一步必须先建立可重复的安全观测环，再做最小修订并单独申请真实复验。

用户随后授权离线候选修复。公开AgentExecutor红测先证明启动参数仍发送legacy `exclude`，最小实现只把这一项替换为canonical `shell_environment_policy.filters={CODEX_HOME="exclude"}`，且测试明确拒绝两种形式并存。本机Codex使用相同严格配置运行`exec --help`返回0，证明当前版本可解析该配置；定向55项、全仓615项（9 skip）、编译和diff-check均通过，真实调用0次。这里刻意只写“候选修复”：无模型help和Fake argv不能观察Agent工具进程环境，只有下一次用户授权的真实smoke才能确认H1，不能用绿测改写首次真实失败。

canonical filters真实复验随后仍失败。第二次使用全新Session且只调用一次，JSONL、shell、turn终态、read-only和Workspace未修改均通过，但最终仍为`env_codex_home_present=true`；耗时16638 ms，输入30598、缓存输入22016、输出163、reasoning输出68。连续两次真实结果一致，足以否定“只是legacy字段导致”的单一假设，但当前报告只保留模型最终短答，没有保留工具原始输出，因此还不能区分CLI工具进程确实继承变量与模型误读。正确的下一步不是继续试配置，而是让Runtime只从工具输出中的固定安全哨兵提取布尔证据，保持原始输出不公开；有了工具观察与模型声称两条证据后，再做最小差分。

该证据缺口随后用两个TDD纵切补上。`CodexCliAgentExecutor`只识别工具输出中的整行固定哨兵`CODEX_RUNTIME_ENV_CHECK codex_home_present=<true|false>`，只公开布尔值，包含路径、值或任意其他噪声的原始输出仍完全丢弃；true和false同时出现时不选择其一。Smoke报告现在以这条Runtime工具观察为安全门槛，模型最终回答只用于一致性检查；测试已证明“工具为true、模型声称false”必须失败。定向58项、全仓618项（9 skip）、编译和diff-check通过，本批没有读取真实环境、认证信息或发起模型调用。下一次真实smoke将能区分实际工具观察和模型陈述，但在执行前先决定是否把环境策略收紧为默认不继承。

环境策略最终选择默认拒绝，而不是继续扩充黑名单。`inherit=core + filter`的优点是兼容性高，但它依赖对“core包含什么”和所有敏感变量名字的持续正确认识；两次真实smoke已经说明“参数里写了排除规则”不能当成实际隔离证据。只使用allowlist filter仍可能受配置层、特殊注入和后续新增变量影响。当前方案改为`inherit=none`，再通过`set`只注入项目固定的安全`PATH`；认证用的`CODEX_HOME`仍只给Codex主进程，并继续保留canonical filter与默认秘密名排除作纵深防御。公共Executor红测先捕获旧`inherit=core`，最小修改后转绿；当前CLI严格解析、定向58项、全仓618项（9 skip）、编译和diff-check通过，真实调用0次。这降低了未知环境变量穿透的风险，但还不能称为安全验收通过：必须等一次单独授权的真实smoke由Runtime直接观察工具环境、权限和写入结果。

真实验收随后严格按上述门槛只运行一次新Session。Runtime从shell工具事件得到唯一`codex_home_present=false`，模型短答一致；read-only Sandbox、Workspace未修改、Session、shell和turn终态均通过，报告退出0。没有resume、重试、第二Agent或第二次调用，也没有公开变量值、认证材料、工具原文、完整stderr或私有推理。这说明`inherit=none + 固定PATH`确实解决了当前Codex认证桥向Agent工具环境穿透的问题，而不是仅让模型换了一种回答。仍需保持边界准确：它关闭的是这一个已复现的安全缺口，不证明所有环境变量、所有权限模式或整个产品达到生产安全。

## DEC-011：工具协议无状态，Agent业务状态显式持久化

### 问题

如果工具或Agent Backend调用依赖当前连接、某个常驻进程或供应商Session中的隐式历史，调用方就无法仅凭请求解释本轮为何执行。连接重建、CLI Session丢失、进程崩溃或Backend替换时，任务进度、权限和记忆也会一起丢失；测试还必须复刻不可见Session，容易出现串线和无法复现。

### 最终策略

借鉴MCP式“协议无状态、业务状态显式化”的思想，但不因这个原则强行引入MCP Server。每次工具或Agent Backend调用都是独立、自描述的请求，不依赖连接局部状态；至少显式绑定`task_id`、`invocation_id`、`snapshot_id`、`permission_snapshot_id`和所需`artifact_id`引用。任务进度、已验证事实、记忆、预算、权限、恢复点和状态版本由Runtime持久化；Context Compiler根据这些引用生成当次最小Payload。

CLI/Provider Session ID仍可用于减少重复上下文和支持自然连续性，但它只是Runtime持有的`SessionBinding`优化，不是业务状态真相。Session不存在、失效或Backend更换时，Runtime必须能从Snapshot、Message和Artifact重建下一次调用；引用不匹配、Snapshot过期或权限版本错误时在调用工具前fail-closed。

工具结果只返回规范公开事件、Artifact引用、证据和候选状态变化，不直接写Runtime任务状态。Runtime验证因果关系、幂等身份、权限和预期状态版本后，才提交新状态并生成下一`Snapshot`。因此Transport可以无连接状态，Runtime仍然是明确有状态且可恢复的。

### 为什么这样选择

这条边界同时降低Session串线、隐藏状态污染和Backend锁定风险，也让重放测试只需固定请求引用与持久快照。它与当前架构并不冲突：Thread/Invocation/Message/Artifact/RoleAssignment本来就由Runtime管理；需要修订的是未来AgentExecutor信封不能只靠`prompt + session_id`表达上下文，必须逐步补齐显式业务引用。

### 实施边界

不立即批量重命名现有Thread/Invocation，不把每条短消息都制造成Artifact，也不为了协议风格提前建设分布式状态服务。下一垂直切片先扩展AgentExecutionRequest的业务引用，通过Fake证明三件事：相同Snapshot可确定重放；Session丢失时能从Runtime Snapshot重建；错误Task/Snapshot/Permission组合在Backend调用前拒绝。真实Codex Session恢复测试随后验证Session只是优化而不是恢复唯一来源。

首个实现切片已经把这一原则落到公共调用边界。`AgentExecutionStateEnvelope`包含typed Task ref、固定content hash的Task Snapshot/Permission Snapshot/Artifact refs和声明权限；`AgentExecutionRequest`不再允许省略该信封。`AgentExecutionRuntime`按Invocation ID读取冻结Authority，缺失返回`state_not_found`，权限或整个信封不等于权威版本返回`state_mismatch`，然后才允许调用AgentExecutor。TDD先证明合法信封能到达Fake一次，再用错误Permission Snapshot复现旧pass-through仍调用Backend，最小精确比较后Fake调用数变为零。定向60项、全仓620项（9 skip）、编译和diff-check通过，真实调用0。这里刻意没有宣称恢复闭环：Authority仍是内存固定映射，SQLite真相、相同Snapshot持久重放和Session丢失后的Context重建留给下一切片。

第二个切片已将Authority与完成结果接入Runtime受管SQLite v7。状态和结果分表持久，使用canonical JSON、digest和append-only trigger防止静默覆盖。测试在第一次Fake Backend完成后重建Database、Store和Runtime，相同Invocation/信封返回原结果，Backend总调用数仍为1；Task、Snapshot、Permission Snapshot或Artifact任一变化都在Backend前拒绝。这证明“已提交完成结果的持久重放”，但不是exactly-once：如果Backend已完成而进程在结果入库前崩溃，当前仍缺少claim/fencing证据来安全决定是重试还是人工介入。Session丢失后的Context重建也仍待下一切片。

## DEC-012：禁止自主进化，所有系统演进由用户逐次批准

### 当前事实与风险

当前项目没有自动Evolver，也没有自动修改Prompt、Role、Profile、Skill、模型/工具策略、Runtime路由或自动采纳评测结论的产品路径。历史`Harness Evolution Protocol`只是开发人员使用Baseline、失败证据、Validation和KEEP/ROLLBACK来优化代码的人工实验纪律；它不等于Runtime自我学习，Plan26也明确写了“不等于允许Agent自主修改生产”。

但“现在没有功能”不等于未来天然安全。当前`workspace-write`只能限制Agent是否能写工作区，Composition签发的一次性批准只证明某个进程获准启动，并不证明用户已经查看和批准了系统自身的精确修改。如果Developer以后在本项目仓库内运行，它理论上能碰到Prompt、策略或Runtime代码；因此仅靠现有Sandbox不能满足“每一次演进都经用户检阅和同意”。

### 最终策略

产品永久禁止自主进化。Agent可以分析失败并提出`ChangeProposal`，但只能停在`PROPOSED / PENDING_USER_REVIEW`。提案必须公开目标、理由、精确diff或配置、影响范围、风险、测试证据和不可变`change_digest`。用户对该精确digest逐次明确批准后，Runtime才能记录一次性`USER_APPROVED`并允许应用；任何正文、目标文件、权限、依赖或digest变化都会使批准失效并重新回到待审。

受保护演进面至少包括Agent Prompt、Role/Profile、模型选择策略、工具和权限、通信/路由/终止/预算规则、Skill、Context/Memory策略、Validator/Acceptance规则以及系统自身代码。Agent投票、Reviewer结论、Validator全绿、评测分数提高、历史KEEP、一次长期授权或“自动优化”开关都不能代替用户批准。Agent不得批准自己的提案，也不得把一次批准复用于后续版本。

### 实施边界

目前先把这条作为产品硬门，不新增任何Evolver。现有只读讨论、评测和提案仍可继续；在产品允许Developer修改本项目自身或上述治理面之前，必须先TDD实现`PROPOSED → USER_APPROVED → APPLIED`状态机、精确digest绑定、变更后批准失效和无批准fail-closed。门禁未实现前，涉及这些面的Agent输出只能保存为只读Proposal Artifact，不能自动写入或激活。
