# Coding Agent Harness：一周学习与项目实践路线

## 1. 目标与边界

### 项目目标

> 构建一个单机、单用户、供应商无关、安全可控、可观察的 Coding Agent Harness。

Harness 是确定性运行底座，负责工作流、状态、权限、上下文、预算、执行和观测；模型只提供概率性决策。

### 一周目标

基于已有的 Python、Java、C++、软件工程、操作系统和软件测试基础，一周内达到：

> 能独立设计、实现、测试、演示并评价一个 L2 级单机 Coding Agent Harness。

一周后应当能够：

- 解释 Harness、Role、Worker、Model、Tool、Memory 的边界。
- 设计状态机、声明式 DAG 和节点契约。
- 处理结构化输出、重试、超时、取消和终止条件。
- 建立基础 Capability 权限和 Runtime 安全检查。
- 设计 Context Projection 和并行结果合并规则。
- 通过 Trace、测试和指标评价项目。

### 暂不学习

- 模型训练、微调和 Transformer 数学
- GraphRAG 和大规模向量数据库
- Kubernetes、MicroVM 和复杂服务网格
- 分布式共识、多租户和大规模集群调度

这些不是当前单机 L2 Harness 的前置条件。

## 2. 核心概念地图

### 2.1 系统角色

| 对象 | 职责 | 不应该负责 |
|---|---|---|
| Harness | 状态、调度、重试、取消、权限、结果合并 | 具体业务推理 |
| Role | 描述一次任务的职责、能力和上下文范围 | 持有模型或执行状态 |
| Worker | 执行一个工作流节点并返回结构化结果 | 修改全局状态 |
| Model | 生成计划、代码或审查意见 | 直接执行系统操作 |
| Tool | 提供文件、命令、网络等外部能力 | 自行决定权限 |
| Memory | 为当前节点投影最小必要上下文 | 无限制传递完整历史 |

核心原则：

```text
Model 提出决策
Harness 校验决策
Gateway 检查权限
Tool 执行操作
Harness 合并结果
```

### 2.2 必学知识

| 主题 | 需要掌握 | 项目落点 |
|---|---|---|
| LLM 工程 | 消息、结构化输出、Token、Tool Calling、Prompt Injection | Model、Backend |
| 工作流 | FSM、DAG、节点契约、重试、幂等、取消 | Harness Core |
| 安全 | 最小权限、路径与命令安全、密钥和资源隔离 | Policy、Gateway、Workspace |
| Memory | Working Memory、Context Projection、上下文预算 | Memory Engine |
| 并发 | 不可变结果、版本、竞态、Reducer | Scheduler、ResultEnvelope |
| 可观测性 | Event、Log、Trace、Metric、Correlation ID | Recorder、Web UI |

学习时始终回答四个问题：

1. 谁拥有状态？
2. 谁可以修改状态？
3. 失败由谁处理？
4. 更换 Worker 或模型时，哪些代码不应该变化？

## 3. 学习与开发方法

学习和项目完善同步进行，两者不是独立任务。每天只选择一个真实问题：

```text
学习概念
   ↓
做最小实验或失败测试
   ↓
把机制应用到项目
   ↓
运行相关测试和完整回归
   ↓
复述原理并记录限制
```

每个主题必须留下：

- 一段自己的概念解释
- 一个明确的失败场景
- 一个或多个自动化测试
- 一个最小项目改进
- 一份测试证据
- 一条已知限制
- 一个小型 Git commit

如果项目本身足以验证概念，可以跳过独立 Demo，直接用项目测试完成最小实验。

## 4. 一周计划

建议每天投入 **4～6 小时**：

- 45～90 分钟：学习当天概念
- 30～60 分钟：设计最小实验和项目方案
- 2～3 小时：测试驱动实现
- 30 分钟：回归、复述和记录

### Day 1：统一语言与架构边界

学习：

- Harness、Role、Worker、Model、Tool、Memory
- 控制面与执行面
- 概率性决策与确定性执行

实践：

- 画出项目依赖图。
- 为核心模块各写一句职责。
- 标记含义混乱或职责重叠的代码。

验收：

- 不看代码，五分钟内讲清完整任务流程。
- 能指出状态、策略和实际执行分别在哪里。

### Day 2：节点契约与模型边界

学习：

- 结构化输出、Schema 校验、Tool Calling
- 模型结果与 Runtime 执行结果的区别
- Token 和上下文边界

实践：

- 定义 `NodeInput`、`NodeResult` 和失败类型。
- 测试缺失字段、错误类型、超长输出和未知字段。

验收：

- 错误模型输出不能绕过 Runtime。
- 节点契约不依赖 DeepSeek 或其他供应商。

### Day 3：状态机与 DAG Executor

学习：

- FSM、DAG、拓扑排序和节点就绪条件
- Retry、Idempotency、Timeout、Cancellation、Checkpoint

实践：

- 定义合法状态迁移表。
- 让 `WorkflowSpec` 真正驱动一部分执行。
- 测试环、缺失依赖、非法迁移、取消和重试。

验收：

- 改 Workflow 配置可以改变执行顺序，不修改 Harness 核心。
- 所有失败都有确定状态，不会无限循环。

### Day 4：权限与安全边界

学习：

- Prompt Injection、最小权限、Capability/RBAC
- 文件、命令、网络、密钥和资源隔离

实践：

- 建立 Role × Capability 权限矩阵。
- 定义 Coding Agent Threat Model。
- 测试路径穿越、危险命令、密钥泄露、超时和过大输出。

验收：

- 权限由 Runtime Policy 强制执行，而不是依赖 Prompt。
- Worker 不能绕过统一执行入口扩大权限。

### Day 5：Memory 与并行结果

学习：

- Working Memory、Context Projection
- 不可变结果、乐观并发、Result Reducer

实践：

- 明确 Task、Project、Role Memory 边界。
- 记录上下文为什么被选择。
- 定义并行结果合并和冲突规则。
- 测试过期结果和读写冲突。

验收：

- Worker 只看到当前节点需要的信息。
- 迟到结果不能覆盖新状态。

### Day 6：可观测性与评价

学习：

- Event、Log、Trace、Metric
- Trace ID、Correlation ID、Critical Path

实践：

- 记录节点开始、完成、失败和取消事件。
- 统计任务成功、首次通过、重试、节点耗时和并行加速比。

验收：

- 不读源码也能通过 Trace 解释一次任务。
- 能指出最慢节点、无效调用和重试原因。

### Day 7：完整闭环与评估

综合测试：

1. 输入简单 Coding 需求。
2. 注入一次实现错误。
3. Tester 与 Reviewer 并行检查。
4. Fixer 根据结构化反馈修复。
5. 重新测试并完成任务。
6. 输出完整事件轨迹和指标。

最终报告：

- 已具备且有测试证明的能力
- 当前安全边界
- 已知限制与未验证假设
- 当前成熟度
- 下一阶段最小目标

验收：能独立演示、解释和评价整个 Harness，并区分“已经实现”与“只有设计”。

## 5. Vibe Coding 协作规则

### AI 可以主要完成

- 检查代码、依赖和调用关系
- 提出少量候选方案并说明取舍
- 生成接口和 Schema 草案
- 编写测试和最小实现
- 运行回归并分析失败
- 更新文档和汇总限制

### 项目负责人必须决定

- 问题范围和验收标准
- 模块、权限和数据边界
- 风险接受程度
- 是否允许 Shell、网络或覆盖文件
- 重试的外部副作用如何处理
- 并发冲突时采用什么规则
- 项目何时达到完成标准

AI 可以分析选择，但不能代替这些产品、安全和架构决策。

### 每日任务提示模板

```markdown
# 今日主题

## 当前问题
- 问题是什么？
- 为什么现在解决？

## 范围
今天只实现：
1. ...

今天不实现：
1. ...

## 执行要求
1. 检查现有实现和测试。
2. 解释概念并给出最多两个方案。
3. 先写暴露问题的失败测试。
4. 实现最小修改。
5. 运行相关测试和完整回归。
6. 说明测试证明了什么、没有证明什么。
7. 记录设计选择和当前限制。

## 验收标准
- ...
- 现有测试全部通过。
- 不修改无关模块。

## 安全约束
- 不扩大文件、命令或网络权限。
- 不读取或记录密钥。
- 不执行破坏性 Git 操作。
```

## 6. 每日完成检查

### 三分钟复述

不看代码回答：

- 今天解决了什么问题？
- 核心机制是什么？
- 状态和权限由谁拥有？
- 失败后会发生什么？
- 当前还有什么限制？

### 代码定位

能够指出：

- 核心接口、策略、执行和测试分别在哪里。
- 修改规则时应该改哪个模块。
- 哪个依赖方向不能反转。

### 行为预测

能够预测：

- 删除某项校验后哪个测试会失败。
- 修改状态后工作流如何变化。
- Worker 越权时由哪层拒绝。
- 模型返回错误结构时在哪里被截断。

回答不出来时，说明代码虽然完成，但当天学习尚未完成。

## 7. 项目评价标准

不要使用 Agent 数量或代码量评价项目。

| 维度 | 核心问题 | 指标示例 |
|---|---|---|
| 正确性 | 是否满足用户要求 | 验收通过率 |
| 稳定性 | 是否稳定完成 | 成功率、重试率 |
| 安全性 | 是否阻止越权 | 攻击测试通过率、违规次数 |
| 可控性 | 是否可停止和限制 | 取消延迟、超时执行率 |
| 可观察性 | 是否能定位失败 | Trace 完整率、定位耗时 |
| 效率 | 并行是否有效 | 总耗时、关键路径、加速比 |
| 成本 | 是否存在浪费 | 成功任务 Token、调用次数 |
| 扩展性 | 新增能力是否影响核心 | 新适配器改动文件数 |
| 可复现性 | 是否能还原过程 | 事件记录完整率 |

## 8. 成熟度与一周终点

| 等级 | 定义 |
|---|---|
| L1 可运行 | 固定流程可以生成、测试和有限返工 |
| L2 可控制 | 有状态、权限、超时、重试、取消和确定性执行 |
| L3 可诊断 | 有 Trace、指标、错误分类和任务回放 |
| L4 可扩展 | Workflow、Role、Worker、Model 可配置并支持安全并行 |
| L5 可生产化 | 具备可靠隔离、恢复、成本治理和多用户能力 |

当前项目处于 **L1 完成、向 L2 过渡**。这一周的终点不是“学完所有 Agent 技术”，而是：

> 通过七个小型、可测试、可解释的项目改进，把当前 Harness 推进到可验证的 L2。
